import { FC } from 'react'
import { Layout, Row } from '@caldwell619/component-toolkit'

const Landing: FC = () => {
  return <Layout seo={{ title: 'Choose a Faction' }}></Layout>
}

export default Landing
